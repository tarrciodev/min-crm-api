name: Deploy to aws
on:
    push:
        branches:
            - main

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - name: Use java
              uses: actions/setup-java@v4
              with:
                  java-version: 17
                  distribution: "temurin"
